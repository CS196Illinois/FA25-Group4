# Core dependencies

# Data processing
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0

# HTTP requests for news APIs
requests>=2.31.0,<3.0.0

# HTML parsing for full article extraction
beautifulsoup4>=4.12.0,<5.0.0

# Ticker validation and stock data
yfinance>=0.2.0,<1.0.0

# Google Gemini AI for LLM operations
google-generativeai>=0.3.0,<1.0.0

# Environment variable management
python-dotenv>=1.0.0,<2.0.0

# HuggingFace Transformers for sentiment analysis
transformers>=4.30.0,<5.0.0
tokenizers>=0.13.0,<1.0.0

# PyTorch CPU-only (for transformers/sentiment model)
# Note: Install separately with: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# torch>=2.0.0
# torchvision>=0.15.0
# torchaudio>=2.0.0
